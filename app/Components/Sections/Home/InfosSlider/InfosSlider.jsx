

export default function InfosSlider({title,images,boxHeight}) {
    const imagesSlider = images.map()
  return (
    <div>
        <TitleBox title={title}/>
        <div>
            
        </div>
    </div>
  )
}

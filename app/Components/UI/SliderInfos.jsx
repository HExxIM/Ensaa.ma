

export default function SliderInfos() {
    const sliderInfos = [
        {

        }
    ];

    const SliderInfosList = sliderInfos.map((info)=>{

    });

  return (
    <>{SliderInfosList}</>
  )
}
